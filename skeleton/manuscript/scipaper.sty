\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{scipaper}[2026/08/21 v1.0 honest gap markers for incomplete drafts]

% \gap{kind}{note} -- kinds: number, cite, q, logic, todo
%   default (draft): renders loudly inline and in the margin
%   [final]:         every invocation raises a package error and the build fails
% The error fires on invocation, never at load: a gap-free manuscript must
% build cleanly under [final], or the mechanism is worthless.

\newif\ifap@final
\DeclareOption{draft}{\ap@finalfalse}
\DeclareOption{final}{\ap@finaltrue}
\ProcessOptions\relax

\RequirePackage{xcolor}
% marginnote, not \marginpar. \marginpar goes through LaTeX's float mechanism,
% which raises "Not in outer par mode" or loses the float outright when the
% marker sits in a minipage, a footnote or a float -- all ordinary places for a
% gap in an astronomy manuscript. \marginnote places the note directly and
% works in every one of them. A marker that breaks the build is worse than a
% marker with no margin note.
\RequirePackage{marginnote}

\makeatletter
% The margin carries the *kind* only, deliberately. The inline marker already
% carries the full note; repeating it in the margin would overflow and compete
% with it. What the margin is for is scanning a page edge and seeing at a glance
% that this page holds two numbers and a citation.
% Still illegal in math mode; there the inline marker stands alone.
\newcommand{\ap@margin}[1]{%
  \ifmmode\else
    \marginnote{\footnotesize\raggedright\textcolor{red}{#1}}%
  \fi}
% Robust in both modes: a \gap in a caption or a section heading is written to
% the .aux and read back, and a fragile macro would expand into formatting
% internals there instead of surviving as itself.
\ifap@final
  \DeclareRobustCommand{\gap}[2]{%
    \PackageError{scipaper}{Unfilled \string\gap{#1} in final mode: #2}%
      {Fill this gap, or drop the [final] option in main.tex to build a draft.}}
\else
  \DeclareRobustCommand{\gap}[2]{\textcolor{red}{\textbf{[GAP #1: #2]}}\ap@margin{#1}}
\fi
\makeatother

% ---------------------------------------------------------------------------
% AAS journal abbreviation macros.
%
% BibTeX from `ads.py export` is verbatim (house rule 2), and ADS writes
% journal names as these macros: journal = {\mnras}. A real journal class
% defines them; the plain `article` class the scaffold starts with does not, so
% without this block the very first exported reference fails the build with
% "Undefined control sequence \apj" -- which makes the verbatim rule unusable
% for as long as the venue is undecided, and that is deliberately most of the
% project.
%
% \providecommand is what makes this safe: a journal class defines these before
% any package loads, so its versions win and only what is still missing gets
% filled in. \AtBeginDocument widens that window to cover a package loaded
% after this one. It is defensive rather than necessary, and it is not a
% guarantee -- a class that defines these from its own \AtBeginDocument hook
% registered after ours would still clash. No astronomy class does.
% ---------------------------------------------------------------------------

\AtBeginDocument{%
\providecommand{\aj}{AJ}%                       Astronomical Journal
\providecommand{\actaa}{Acta Astron.}%
\providecommand{\araa}{ARA\&A}%                 Annual Review of A\&A
\providecommand{\apj}{ApJ}%                     Astrophysical Journal
\providecommand{\apjl}{ApJ}%                    ApJ Letters
\providecommand{\apjs}{ApJS}%                   ApJ Supplement
\providecommand{\ao}{Appl.~Opt.}%
\providecommand{\apss}{Ap\&SS}%
\providecommand{\aap}{A\&A}%                    Astronomy and Astrophysics
\providecommand{\aapr}{A\&A~Rev.}%
\providecommand{\aaps}{A\&AS}%
\providecommand{\azh}{AZh}%
\providecommand{\baas}{BAAS}%
\providecommand{\bac}{Bull. astr. Inst. Czechosl.}%
\providecommand{\caa}{Chinese Astron. Astrophys.}%
\providecommand{\cjaa}{Chinese J. Astron. Astrophys.}%
\providecommand{\icarus}{Icarus}%
\providecommand{\jcap}{J. Cosmology Astropart. Phys.}%
\providecommand{\jrasc}{JRASC}%
\providecommand{\mnras}{MNRAS}%                 Monthly Notices of the RAS
\providecommand{\memras}{MmRAS}%
\providecommand{\na}{New A}%
\providecommand{\nar}{New A Rev.}%
\providecommand{\pra}{Phys.~Rev.~A}%
\providecommand{\prb}{Phys.~Rev.~B}%
\providecommand{\prc}{Phys.~Rev.~C}%
\providecommand{\prd}{Phys.~Rev.~D}%
\providecommand{\pre}{Phys.~Rev.~E}%
\providecommand{\prl}{Phys.~Rev.~Lett.}%
\providecommand{\psj}{Planet. Sci. J.}%
\providecommand{\pasa}{PASA}%
\providecommand{\pasp}{PASP}%
\providecommand{\pasj}{PASJ}%
\providecommand{\raa}{Res. Astron. Astrophys.}%
\providecommand{\rmxaa}{Rev. Mexicana Astron. Astrofis.}%
\providecommand{\qjras}{QJRAS}%
\providecommand{\skytel}{S\&T}%
\providecommand{\solphys}{Sol.~Phys.}%
\providecommand{\sovast}{Soviet~Ast.}%
\providecommand{\ssr}{Space~Sci.~Rev.}%
\providecommand{\zap}{ZAp}%
\providecommand{\nat}{Nature}%
\providecommand{\iaucirc}{IAU~Circ.}%
\providecommand{\aplett}{Astrophys.~Lett.}%
\providecommand{\apspr}{Astrophys.~Space~Phys.~Res.}%
\providecommand{\bain}{Bull.~Astron.~Inst.~Netherlands}%
\providecommand{\fcp}{Fund.~Cosmic~Phys.}%
\providecommand{\gca}{Geochim.~Cosmochim.~Acta}%
\providecommand{\grl}{Geophys.~Res.~Lett.}%
\providecommand{\jcp}{J.~Chem.~Phys.}%
\providecommand{\jgr}{J.~Geophys.~Res.}%
\providecommand{\jqsrt}{J.~Quant.~Spec.~Radiat.~Transf.}%
\providecommand{\memsai}{Mem.~Soc.~Astron.~Italiana}%
\providecommand{\nphysa}{Nucl.~Phys.~A}%
\providecommand{\physrep}{Phys.~Rep.}%
\providecommand{\physscr}{Phys.~Scr}%
\providecommand{\planss}{Planet.~Space~Sci.}%
\providecommand{\procspie}{Proc.~SPIE}%
}
